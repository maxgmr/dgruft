//! The terminal interface (TI). TODO
use color_eyre::eyre;

/// The terminal interface (TI). TODO
pub struct Ti {
    // TODO
}
impl Ti {
    /// Create a new [Ti].
    pub fn new() -> eyre::Result<Self> {
        // TODO
        Ok(Self {})
    }
}
